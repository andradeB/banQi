import styled from 'styled-components/native';
import {Container, ContainerProps} from '../Container';

export const CardContainerContainer = styled(Container)<ContainerProps>`
  elevation: 2;
  box-shadow: 0px 2px 4px rgba(0, 0, 0, 0.15);
`;
